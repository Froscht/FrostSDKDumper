================================================================
FrostSDKDumper — TODO (2026-05-07 → next session)
================================================================

CURRENT PATCH: CL-1177678 (binary
  ARC_RAIDERS_STEAM_MERGED_20260505_120058_77PagesDecrypted.exe)
Previous in-tree state was CL-1177146 — see archived section
at the bottom of this file for that history.


WHAT SHIPPED 2026-05-07 (Theia resolution system — IMPL ONLY)
-------------------------------------------------------------
Built a complete optional resolution layer aimed at the
remaining ~2.9% of properties that come back as Theia opaque
placeholders. Headers added (NOT yet wired into main.cpp):

  1. theia_blake3_extractor.h — CityHash64WithSeed dictionary
     attack with BLAKE3 magic IV 0x666CC71CF1242CBA. Built-in
     dictionary of ~100 UE5 standard names. Class:
     TheiaResolver::TheiaBlake3Extractor, methods
     ExtractSeedFromMemory / BuildHashDictionary /
     ResolveOpaqueHash / ResolvePlaceholders.

  2. theia_pattern_scanner.h — 16-byte key pattern recognition.
     Class TheiaResolver::TheiaPatternScanner with COMPLEX/MASK
     decryption variants. Known key patterns from EAAntiCheat
     ("nx0iddGJKTuY8aPM" etc.) hardcoded as starting set.

  3. optimized_batch_processor.h — batch CI resolution wrapped
     around the discovered CL-1177678 functions:
       FField_DecryptNamePrivate_CL1177678  @ 0x353830
       FName_Index2Name_CL1177678           @ 0x2311B0
       FName_ResolveCI_ToString_CL1177678   @ 0x22EF40
     Adds memory-locality sort + cache. Class:
     FrostSDK::OptimizedBatchProcessor.

  4. theia_master_resolver.h — coordinator running 4 phases
     (BLAKE3 dict / pattern scan / batch CI / Epic FNAME_Find
     fallback). Class FrostSDK::TheiaMasterResolver, struct
     ResolutionStatistics for tracking, method
     ExecuteFullResolution.

  5. sdk_generator_theia_integration.h — drop-in subclass
     SDKGen::TheiaIntegratedGenerator : public Generator that
     overrides BuildSDK() to extract placeholders, run the
     master resolver, and rewrite output.

  6. test_theia_resolution.cpp — standalone test harness with
     mock placeholders, per-component tests, and a 10K-entry
     benchmark. UNTESTED — see "TEST ON WINDOWS" below.

  7. IMPLEMENTATION_GUIDE.md — integration recipes (Option 1
     1-line swap, Option 2 manual master resolver call,
     Option 3 component-by-component).

Reference docs added in same pass:
  - status_06.05.26.md  (CL-1177678 RE state, 17 fn renames,
    7 globals, decoder ecosystem)
  - complete_function_analysis_06.05.26.md  (21-fn deep dive)
  - theia_resolution_strategy.md  (3-phase strategy synthesis
    from leaked source dumps)


PENDING — INTEGRATION & VALIDATION (priority order)
---------------------------------------------------
P0  Wire TheiaIntegratedGenerator into main.cpp.
    Single-line swap per IMPLEMENTATION_GUIDE.md Option 1:
      SDKGen::Generator generator;
      → SDKGen::TheiaIntegratedGenerator generator;
    Verify build is still clean. Default behavior should be
    no-op + statistics print when there are no placeholders.

P0  Add the 7 new headers to FrostDumper.vcxproj
    <ClInclude> group + test_theia_resolution.cpp to
    <ClCompile> if we want it as a buildable test target.
    (CLAUDE.md rule: missing files cause unresolved-extern
    linker errors, easy to miss.)

P0  TEST ON WINDOWS via the heredoc + cmd //C build pattern
    documented in CLAUDE.md "Building C/C++ Projects".
    The test harness writes printf-style output; capture
    stdout on a real run and check:
      - BLAKE3 dictionary builds without crash
      - Mock CI 505 → "Object" / 506 → "World" etc. resolve
      - Master resolver Phase 1-4 stats print
      - 10K-entry benchmark completes <5s

P1  Live in-game validation. With game running on
    CL-1177678, run dumper with TheiaIntegratedGenerator
    enabled. Check:
      - Reports at SDK_Output.txt / Theia_Resolution_Report.txt
        / Detailed_Analysis_Report.txt are emitted
      - Resolution rate goes from baseline 97.1% → ≥99%
      - No regression on already-resolved properties
      - Total runtime stays under ~1.5× the pre-Theia run

P1  CityHash64WithSeed implementation in
    theia_blake3_extractor.h needs unit-validation against a
    known reference (e.g. one of the leaked Epic samples in
    /media/frost/Coding Stuf/Infos/theia*.txt). The constants
    k0=0xc3a5c85c97cb3127 / k1=0xb492b66fbe98f273 /
    k2=0x9ae16a3b2f90404f are correct, but the mixing /
    final-mix steps need a sanity check before we trust the
    ResolveOpaqueHash output.

P2  Function-pointer addresses inside OptimizedBatchProcessor
    (FunctionPointers struct) are hardcoded RVAs from
    CL-1177678. They need:
      - A live verification pass (read first byte at each
        address, compare against the IDA prologue) before any
        of the cross-process call attempts is allowed to fire
      - Auto-rediscovery hook — string-anchor the same way
        status_06.05.26.md found them, so the next patch
        doesn't silently break this codepath

P2  Test suite is g++/Linux. We're MSVC/Windows. Either:
      - Build it as a console subproject under FrostDumper.sln
        (preferred — exercises the same toolchain), or
      - Strip it out of the production tree and keep it as a
        ref-only sample.


KNOWN ISSUES
------------
- Slot-decoder + FField offset shift to CL-1177678 already
  shipped (see discovery_cl1177678_slot_decoder_solved.md and
  discovery_cl1177678_ffield_offsets.md memories). Current
  end-to-end stat baseline before Theia layer:
    127,220 names @ 100% naming, 4004 structs + 2878 enums,
    Properties 102K — i.e. ~37% of the reference SDK target.
    Hybrid refSDK overlay (tools/sdk_postprocess.py) lifts
    effective coverage to ~95%. Theia layer is meant to lift
    the LIVE-only coverage closer to refSDK without the
    overlay step.

- No-op runs of the new resolver should be free (early-out
  when Placeholders is empty) but this hasn't been verified.
  Confirm before merging — we don't want it adding seconds
  to every dump even when there's nothing to resolve.

- Pattern scanner relies on at least one of the EAAntiCheat
  16-byte key strings still being resident. Not validated on
  the CL-1177678 pages — may need a pattern refresh.

- Epic FNAME_Find fallback is a heuristic. On the lookup
  list it MAY rename a real placeholder to a wrong but
  plausible UE5 name. Need a confidence gate so fallback
  results are tagged in the SDK output (e.g. /* heuristic */
  comment) instead of silently overwriting OriginalName.


NEXT-SESSION QUICK START
------------------------
1. Read CLAUDE.md project root section for CL-1177678 layout
   constants — auto-loaded but worth re-reading the
   "FField/FProperty offsets" + "engine type-pool vtable
   RVAs" lines before touching any decode code.
2. Open IMPLEMENTATION_GUIDE.md and pick Option 1 (1-line
   swap) for first integration.
3. Build via the heredoc .bat → cmd //C pattern from
   CLAUDE.md. Do NOT try inline cmd //C "VsDevCmd.bat &&
   msbuild" — it hangs. Rebuild as soon as any header is
   added to vcxproj.
4. Run dumper against live game; collect baseline stats from
   one pre-Theia run + one post-Theia run for comparison.
5. If resolution rate doesn't move ≥1.5pp, the BLAKE3 seed
   is the most likely culprit. Validate
   CityHash64WithSeed + dictionary build with a known input
   first.
6. NEVER spawn parallel memory-probe work — kernel
   rate-limit overflow caused a 14-min hang last time.
   Serialize all /dev/memreader access. IDA MCP work is fine
   to parallelize.


================================================================
ARCHIVED (CL-1177146, 2026-05-02 → 2026-05-04 history)
================================================================

WHAT SHIPPED 2026-05-03 (Option B — partial; only WIN bump kept)
----------------------------------------------------------------
gobjects.h::ScanPass — 1 change kept:
  1. WIN bumped 0x10000 → 0x400000 (64KB → 4MB). 64× fewer
     process_vm_readv calls for the bulk page reads.

REVERTED (load-bearing):
  - Per-slot vtable validation `m_reader.Read(obj_ptr, &vt, 8)`
    inside the detection loop. Dropping it broke counts:
      Classes 14627 → 9347, Structs 7946 → 5101,
      Enums 2881 → 1186, Functions 40826 → 23295,
      Properties 180828 → 330121 (UP — confirms BPGCs in
      heavy-property regions dominated the cap, metaclass
      chunks got crowded out).
  - Sparse-density filter — added as predicted mitigation but
    threshold was too leaky AND filter rejected real chunks:
    Pass A (MAX_GAP=0) splits real chunks at legitimate null
    slots without vtable filtering, so Pass A produced 21 noise
    runs that all failed density. Pass B kept 22 of 92K+ runs
    (largest=84733) but emitted only 46688 (vs ~70K NumElements).

Final structural scan: still does per-slot vtable check (same
as before). Just reads pages 4MB at a time instead of 64KB.

SECONDARY FIX (same day, 2026-05-03):
  Initial WIN=4MB version had a region-tail bug — `page + WIN
  <= rg.hi` skipped any heap region smaller than 4MB. Fixed by
  clamping the per-iteration read to `min(WIN, rg.hi - page)`
  and the inner offset cap to `this_win`.

WHAT SHIPPED 2026-05-02
-----------------------
1. arc_decrypt.h — Offsets:: namespace flipped from constexpr
   to inline mutable globals so probe phases can overwrite at
   runtime. 108 consumer sites unchanged.
2. auto_offsets.h (NEW, ~600 lines) — Phase 9-15 live-probe
   layout discovery (joint UStruct::ChildProperties +
   FField::ClassPrivate, NamePrivate via hi64 dispersion,
   UStruct::SuperStruct + PropertiesSize, UEnum::Names,
   UClass::FuncMap_*, UFunction::NativeFunc/FunctionFlags,
   FProperty subclass sub-pointers, ArrayDim/ElementSize).
3. fname_decrypt.h — added SetEmuPrimary(bool) flag.
4. main.cpp — Phase 0.6 sanity check + Phase 2 fallback.
5. main.cpp — TryInitViaVtable7 short-circuits on plain
   NumElements layout.
6. auto_discovery.h — depth-2 BFS for chunks-array pointer
   (doesn't find it on CL-1177146).
7. auto_chunks_emu.h (NEW, ~350 lines) — Tier 1 function
   emulation.

WHAT SHIPPED 2026-05-04 (Option A — Tier 1 emulation works)
-----------------------------------------------------------
auto_chunks_emu.h:
  1. Cap lifted 12 → 256 distinct fn_starts.
  2. Walkback validation via LooksLikePrologue, retries up to
     6× with previous CC.
  3. SIMD pattern coverage extended for xmm4-7 (MOVDQU /r=25/
     2D/35/3D, MOVAPS /r=05..3D, MOVUPS /r=05..3D).

  Result on PID 6093 (in-match):
    789 rip-rel loaders found targeting GUObjectArray+0x180
    256 distinct candidate fns
    SUCCESS via fn @ rva=0x422856 — chunks_manager=
      0x297F0F10  chunks_array=0x6AE53D60  vt[3] blob_off=+0x90
    canonical enumeration: 125871 unique UObjects (~70592 expected)

  Final SDK on CL-1177146:
    18284 classes / 10915 structs / 2878 enums /
    55354 functions / 198772 properties
    126775 named, 251 failed (99.8%)

  After enum-gate tightening (sdk_generator.h Path A/C):
    Removed ~167 pseudo-enums; real enums intact.

  Reference target (CLAUDE.md §6 "Reference"):
    21453 / 9355 / 2340 / 41093 / 275843
    Structs/enums/functions exceed reference;
    classes 85% of target, properties 72%.

OPEN ITEMS (CL-1177146, may still be relevant)
----------------------------------------------
- Class vtable cluster oracle floor too high for KCLASS_NATIVE.
- WBPGC autodisc found 0xC199910 (sibling type? unverified).
- Phase 3 FProperty Offset XOR plausibility cross-check.
- Phase 1 vtable cluster scoring depends on random sample luck.
- Legacy SIMD tables (cidxXor1, blkSlotShuf, etc.) read OOB on
  CL-1177146 — stale 20260421 RVAs.
- Option C: PE on-disk fallback for autoemu cold-page recovery
  — not currently set up.
